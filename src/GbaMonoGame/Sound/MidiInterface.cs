namespace GbaMonoGame;

// Only implemented on GBA
public static class MidiInterface
{
    public static MidiParams MidiParams { get; set; }

    public static void SetMidiParams()
    {
        // Pushes the params into the MP2K sound engine
    }
}